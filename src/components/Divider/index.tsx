const Divider = () => {
  return (
    <div className="w-full h-[1px] my-4 bg-gradient-to-r from-transparent via-gray-400 to-transparent dark:via-gray-600"></div>
  );
};

export default Divider;
// This Divider component is a simple horizontal line that can be used to separate content in a layout.
